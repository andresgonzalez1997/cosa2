SELECT DISTINCT
    characteristic_name,
    characteristic_description
FROM prd_product_anh_internal_datablocks.tca_material_characteristics_assignments
WHERE
       UPPER(characteristic_name) LIKE '%GR%'
    OR UPPER(characteristic_description) LIKE '%GR%'
ORDER BY
    characteristic_name,
    characteristic_description;
